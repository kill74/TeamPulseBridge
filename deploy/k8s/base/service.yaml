apiVersion: v1
kind: Service
metadata:
  name: ingestion-gateway
spec:
  selector:
    app.kubernetes.io/name: ingestion-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP

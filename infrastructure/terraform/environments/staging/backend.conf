# Staging Backend Configuration
# Run: terraform init -backend-config=./environments/staging/backend.conf

bucket         = "my-org-terraform-state"
prefix         = "teampulse/staging"
encryption_key = "StagingBase64EncodedKey=="

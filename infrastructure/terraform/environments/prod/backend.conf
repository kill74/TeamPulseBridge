# Production Backend Configuration
# Run: terraform init -backend-config=./environments/prod/backend.conf

bucket         = "my-org-terraform-state"
prefix         = "teampulse/prod"
encryption_key = "ProductionBase64EncodedKey=="

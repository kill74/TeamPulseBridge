apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingestion-gateway
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: ingestion-gateway
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: '1'
              memory: 1Gi

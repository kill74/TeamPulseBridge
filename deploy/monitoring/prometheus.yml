global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: ingestion-gateway
    metrics_path: /metrics
    static_configs:
      - targets:
          - ingestion-gateway:8080
